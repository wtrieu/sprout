import LandingExperience from "@/components/landing/LandingExperience";

export const metadata = {
  title: "Sprout — Every big story starts this small",
  description:
    "A self-hosted family companion for the first years — bedtime stories, cited answers, activities, and a memory of it all. Running quietly at home.",
};

export default function LandingPage() {
  return <LandingExperience />;
}
